<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DirectSql">
    <select id="list" parameterType="java.util.Map" resultType="rcpMap">
        /* DIRECT-SQL.xml - "DirectSql".list */
        ${queryStr}
    </select>

    <select id="get" parameterType="java.util.Map" resultType="rcpMap">
        /* DIRECT-SQL.xml - "DirectSql".list */
        ${queryStr}
    </select>

	<insert id="insert" parameterType="java.util.Map">
		/* DIRECT-SQL.xml - "DirectSql".insert */
        ${queryStr}
	</insert>

	<update id="update" parameterType="java.util.Map">
		/* DIRECT-SQL.xml - "DirectSql".update */
        ${queryStr}
	</update>

	<delete id="delete" parameterType="java.util.Map">
		/* DIRECT-SQL.xml - "DirectSql".delete */
        ${queryStr}
	</delete>
</mapper>